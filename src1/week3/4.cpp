#include <vector>
#include <algorithm>
#include <iostream>

using namespace std;

int main() {
	vector<int> v = {1, 3, 2, 5, 4};
//	int cnt = count_if(begin(v), end(v), [](int x){return x % 2 != 0;});

//	int cnt = 0;
//	for (const auto& i : v) {
//		cnt += (i % 2 != 0);
//	}
//	cout << cnt << endl;
}